<%= form_for(@option) do |f| %>
  <% if @option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
      <% @option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :option_label %><br>
    <%= f.text_field :option_label %>
  </div>
  <div class="field">
    <%= f.label :option_result %><br>
    <%= f.check_box :option_result %>
  </div>
  <div class="field">
    <%= f.label :imagequestion_id %><br>
    <%= f.text_field :imagequestion_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
